process GEN = {

#include "Configuration/Generator/data/PythiaMinBias.cfi"
include "GeneratorInterface/Pythia6Interface/data/PythiaProducerMinBias.cfi"

  untracked PSet maxEvents = {untracked int32 input = 1000}

    # Valid pythiaPylistVerbosity levels
    # are: 0 (none), 1, 2, 3, 5, 7, 11, 12, 13
    replace pyth.pythiaPylistVerbosity = 0 
   ### replace pyth.maxEventsToPrint = 10
    replace pyth.pythiaHepMCVerbosity = false 

   service = Timing{}

    #this sets the random number seed used by Pythia
    service = RandomNumberGeneratorService {
      PSet pyth = {
	untracked uint32 initialSeed = 123456789 
      }
#   untracked string saveFileName = "SaveState.dat"
    }

    source = EmptySource {}

    path p1 = {pyth}

    module out = PoolOutputModule { 
	untracked string fileName = "mcpool2.root" 	
    }
    endpath p = { out }
}
